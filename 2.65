#include<stdio.h>

//异或x的前若干位和后若干位
//每个位的值表示原来位中个数的奇偶
int odd_ones(unsigned x)
{
	x ^= x >> 16;
	x ^= x >> 8;
	x ^= x >> 4;
	x ^= x >> 2;
	x ^= x >> 1;
	return x & 1;
}
int main() {
	int x;
	scanf("%d", &x);
	printf("%d", odd_ones(x));
	return 0;
}
